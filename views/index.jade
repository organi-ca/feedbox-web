extends layout

block content
  h1= title
  p Welcome to #{title}

    form#uploadForm(enctype='multipart/form-data', action='/api/feedbacks', method='post')
      input(type='file', name='file')
      input(type='submit', value='Upload Image', name='submit')
